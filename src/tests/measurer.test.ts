import { describe, it } from 'vitest';
// import { createTspan } from '$lib/svg/measurer.js';

describe('measurer', () => {
	it('createTspan', () => {
		// TODO do this in playwright/cypress
		// const t = createTspan('hi');
		// expect(t.textContent).toBe('hi');
	});
});
